<p id="notice"><%= notice %></p>

<h1>My Comments</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td class="list-group-item"><%= comment.body %> <%=link_to fa_icon('eye'), post_path(comment.post_id) %></td>
<!--        <td><%#= link_to 'Edit', edit_comment_path(comment) %></td>-->
<!--        <td><%#= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

